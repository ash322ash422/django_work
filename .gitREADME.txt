steps
1) On GitHub create new repo 'django_work'
2) On local machine, in the directory 'django_work', type
  - git init
  - git remote add origin https://github.com/ash322ash422/django_work.git
  - git add .
  - git commit -m "Initial commit"
  - git push -u origin master
